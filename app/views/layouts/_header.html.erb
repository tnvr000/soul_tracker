
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Souls', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if authenticated? %>
          <li class="nav-item">
            <%= link_to 'Heroes', 'heroes_path', class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Equipments', 'equipments_path', class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Out', session_path, data: { 'turbo-method' => :delete }, class: 'nav-link primary-color' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In', new_session_path, class: 'nav-link primary-color' %>
          </li>
        <% end %>
      </ul>
    </div> <!-- collapsible navbar items -->
  </div>
</nav>
<div style="margin-block: 15px"></div>
